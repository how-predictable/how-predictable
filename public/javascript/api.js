(function() {
	'use strict'
	
	var keys = undefined;
	
	apiFunctions = {};
	
	apiFunctions.getData = function() {
		
		// Make sure that we have the key, otherwise return
		// undefined
		if (key === undefined) {
			return undefined;	
		}
		
		// Access the api and return a call back
	}
	
	return apiFunctions;
})()